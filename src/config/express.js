import express from 'express';

/**
 * Express application instance.
 * This is the main application object that will be used to:
 * - Configure middleware
 * - Define routes
 */
const app = express();

export default app;

